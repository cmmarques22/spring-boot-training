server:
  port: 8080


spring:
  datasource:
    username: cmmarques22
    password: cmmarques22
    url: jdbc:postgresql://localhost:5332/movie

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
  main:
    web-application-type: servlet

